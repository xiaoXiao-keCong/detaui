import type { App, Plugin } from 'vue';
import {{ compClassName }} from './src';

export const {{ compClassName }}Plugin: Plugin = {
  install(app: App) {
    app.component('deta-{{ regisiterCompName }}', {{ compClassName }});
  },
};

export {
  {{ compClassName }},
};